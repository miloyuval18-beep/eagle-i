# Copy this file to .env and fill in your key. Never commit the real .env file.
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: set both to require an HTTP login before the site loads at all.
# Leave both unset for open access (e.g. local dev).
SITE_USER=
SITE_PASSWORD=
