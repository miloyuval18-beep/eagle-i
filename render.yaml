services:
  - type: web
    name: eagle-i
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: SITE_USER
        sync: false
      - key: SITE_PASSWORD
        sync: false
